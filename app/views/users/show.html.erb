List all of the this users dogs in the table!


<table>
  <tr>
    <th> Dog Name </th>
    <th> Dog Breed </th>
    <th> Age </th>
  </tr>

  <tbody>
        <tr><% @user.dogs.each do |dog| %>
        <td><%= dog.name %></td>
        <td><%= dog.breed %></td>
        <td><%= dog.age %></td>
      </tr>
            <% end %>
      </tbody>
</table>
